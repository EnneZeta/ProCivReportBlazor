@page "/"
@inject Services.ToastService toastService

<div class="mainContainer">
    <div class="row">
        <div class="col-lg-2 col-md-auto"><img src="vpcc_cyan_logo.jpg" width="100%"></div>
        <div class="col-lg-10 col-md-auto">
            <span style="font-family: 'Segoe UI'; font-weight: bold; color: #365dbc; font-size: 4vw; text-align:center">VOLONTARI PROTEZIONE CIVILE DI CALDERARA DI RENO</span>
        </div>
    </div>

    @*<button class="btn btn-info" @onclick="@(() => toastService.ShowToast("I'm an INFO message", Services.ToastLevels.Info))">Info Toast</button>
    <button class="btn btn-success" @onclick="@(() => toastService.ShowToast("I'm a SUCCESS message", Services.ToastLevels.Success))">Success Toast</button>
    <button class="btn btn-warning" @onclick="@(() => toastService.ShowToast("I'm a WARNING message", Services.ToastLevels.Warning))">Warning Toast</button>
    <button class="btn btn-danger" @onclick="@(() => toastService.ShowToast("I'm an ERROR message", Services.ToastLevels.Error))">Error Toast</button>*@

</div>






